import { OverviewNode } from '../../model/class/overview';
export declare class ExamplesParser {
    getNodes(example: any): OverviewNode[] | undefined;
    private parseInlineTag;
    private parseLiveTag;
    private parseStackedTag;
    private parseAdditionalTag;
    private parse;
    private prepareExample;
    private createNodes;
    /**
     * Parses with the following pattern:
     * inline-example(name, example-id, lang, row-start, row-end)
     *
     * @example:
     * inline-example(Super example, some-component/some.component.ts, 21, 46)
     * inline-example(Super example, some-component/some.component)
     * */
    private parseInlineExample;
    /**
     * Parses with the following pattern:
     * inline-example(name, example-id)
     *
     * @example:
     * inline-example(Super example, some-component/some.component)
     * */
    private parseExample;
    private parseRaw;
    private handleName;
}
