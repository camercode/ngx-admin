import { Model, Class, Metadata } from '../model';
import { StylesParser, PropertiesParser, ExamplesParser, MethodsParser, Common } from './parsers';
export declare class DocJsParser {
    protected json: any;
    protected styles: StylesParser;
    protected props: PropertiesParser;
    protected examples: ExamplesParser;
    protected methods: MethodsParser;
    protected common: Common;
    parse(json: any, metadata: Metadata): Model;
    saveJSON(json: any): void;
    getClasses(json: any[]): Class[];
    parseClass(obj: any): Class;
    isClass(obj: any): boolean;
}
